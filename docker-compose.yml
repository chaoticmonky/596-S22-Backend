version: '3.8'

# Setting up the PostgreSQL DB Container
services:
  database:
    image: 'postgres:latest'
    ports: 
      - 5500:5500
  
    # The location we use to share all of our secrets
    env_file:
      - .env
    
    volumes:
      - ./db-data/:/var/lib/postgresql/data/
  
  server:
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
      - ./be-data/:/backend/
    command: uvicorn app.main:app --host 0.0.0.0 --port 8001
    env_file:
      - .env
    ports:
      - 8001:8001
